/**
 * Uygulama yapılandırması.
 *
 * API_URL backend adresidir. Tüm istekler axios ile bu adrese gider.
 * iOS Simulator + HTTP: localhost doğrudan Mac'e erişir, sertifika sorunu olmaz.
 * Backend'in portu farklıysa burayı güncelleyin (ASP.NET HTTP varsayılanı 5000).
 */
export const API_URL = 'http://192.168.1.101:5000';
